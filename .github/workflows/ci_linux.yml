name: CI (Linux)

on:
  push:
    branches: [ smp73]
  pull_request:
    branches: [ smp73 ]

jobs:
  job:
    uses: ./.github/workflows/job-compile-and-test.yml
    with:
      runner-env: ubuntu-22.04
      platform: linux
